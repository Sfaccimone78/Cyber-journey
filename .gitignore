# === OneDrive / Windows ===
# Config cartella OneDrive/Explorer
desktop.ini
# Cache anteprime
Thumbs.db
ehthumbs.db
ehthumbs_vista.db
# File temporanei Office (lock mentre un doc è aperto)
~$*
*.tmp
# Cestino e metadati sistema
$RECYCLE.BIN/
*.lnk

# === macOS (se sincronizzi da Mac) ===
.DS_Store
.AppleDouble
.Spotlight-V100
.Trashes

# === Editor / tool ===
# Config app + codice plugin Obsidian (per-macchina, non necessari nel repo)
.obsidian/
.vscode/
